import 'package:flutter/material.dart';

/// Bảng màu dùng chung toàn app - đổi màu thương hiệu chỉ cần sửa ở đây.
class AppColors {
  AppColors._();

  static const primary = Color(0xFF7C5CFC);
  static const background = Color(0xFFFFF8F0);

  static const idleBubble = Color(0xFFE9D8FF);
  static const listeningBubble = Color(0xFFB2F2E5);
  static const thinkingBubble = Color(0xFFFFE8B3);
  static const talkingBubble = Color(0xFFCDE7FF);
  static const happyBubble = Color(0xFFFFD6E8);

  static const disabled = Color(0xFFBBBBBB);
  static const textMuted = Colors.black54;
}
